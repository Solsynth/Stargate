# Secrets and runtime assets — copy from the C# deployment's Keys/ dir
Keys/

# Local config overrides
config.toml

# Redis snapshots (local dev)
dump.rdb
*.rdb

# Build output (make build → bin/, direct go build → root binaries)
bin/
/stargate
/stargate-migrate

# Go
*.test
